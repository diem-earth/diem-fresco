# production/masters/ — archival masters

Full-resolution lossless masters of the composited fresco (formats TBD in
`../specs/fresco.yaml`). Empty until compositing produces validated output.
Every master here requires a record in `../validation/`.
