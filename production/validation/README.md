# production/validation/ — validation records

One record per master/print artifact, proving the checks in
`../specs/fresco.yaml#validation` passed (ink integrity, exact dimensions,
mask disjointness). Machine-generated where possible, in the spirit of
`qa/reproduce_master.py`. Empty until compositing starts.
