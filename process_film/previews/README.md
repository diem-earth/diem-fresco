# process_film/previews/ — canonical stage previews

Empty until render scripts exist. Every file here must follow the full-canvas
rules in [../README.md](../README.md): complete 34048×5312 composition, one
global downsample factor, panel-local sources composited at global offsets,
RGB PNG on white. Paths are dictated by `../manifest/stages.json#preview`.
